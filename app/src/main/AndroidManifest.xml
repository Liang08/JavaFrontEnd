<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.java.xuhaotian">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        android:usesCleartextTraffic="true">

        <activity android:name=".user.RegisterActivity"></activity>
        <activity android:name=".ChannelSelectActivity" >

        </activity>

        <activity
            android:name=".user.FavouriteActivity"
            android:exported="true" />
        <activity
            android:name=".user.HistoryActivity"
            android:exported="true" />
        <activity
            android:name=".user.ModifyPasswordActivity"
            android:exported="true" />
        <activity android:name=".mainpage.MainPageActivity" />
        <activity android:name=".user.LoginActivity" />

        <activity android:name=".SearchResultActivity"></activity>

        <activity android:name=".SearchActivity" />

        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>